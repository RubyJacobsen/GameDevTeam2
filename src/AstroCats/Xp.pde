class Xp {
  
  float x, y, direction;

  Xp(float x, float y) {
  this.x=y;
  this.x=y;
  }
  void display() {
  rect(x,y,10,10);
  
  }
  void move(float tx, float ty) {
    atan2(tx-x,ty-y);
    
  
  }
}
